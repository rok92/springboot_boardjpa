# server port ?? ??
server:
  port: 8082

# data base ?? ??
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/db_bootboard?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: user_bootboard
    password: 1234

  # thymeleaf ?? ??
  thymeleaf:
    cache: false

  # spring data jpa ??
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        default_batch_fetch_size: 100
    show-sql: true
    database: mysql